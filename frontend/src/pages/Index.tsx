import { useLocation, useNavigate } from "react-router-dom";

// This page is no longer used — the app uses DashboardLayout with nested routes.
// Redirecting to overview for safety.
export default function Index() {
  return null;
}
